interface:
  display_name: "交付闭环"
  short_description: "在独立 Review 与定点修订之间循环，直到交付包 ready"
  default_prompt: "Use $delivery-loop to review and revise this PRD or Product Delivery Package until it is ready or reaches a human gate."
policy:
  allow_implicit_invocation: false
